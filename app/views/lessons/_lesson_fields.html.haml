.nested-fields.border.rounded.p-2.mb-3
  = f.input :title
  = f.input :content, as: :rich_text_area

  -# remove upload video for lessons, because expensive hosting :)
  -# = f.input :video
  -# - if @lesson.video.attached?
  -#   %span.mt-0= @lesson.video.filename
  -#   = link_to "Delete video", delete_video_course_lesson_path(@course, @lesson),method: :delete,
  -#             data: { confirm: "Are you sure?" }

  .form-actions.mt-3
    = link_to_remove_association "Delete lesson", f, class: "btn btn-sm btn-danger"
