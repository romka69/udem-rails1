%h1 Listing lessons

%table
  %thead
    %tr
      %th Title
      %th Content
      %th Course
      %th
      %th
      %th

  %tbody
    - @lessons.each do |lesson|
      %tr
        %td= lesson.title
        %td= lesson.content
        %td= lesson.course.title
        %td= link_to 'Show', course_lesson_path(lesson.course, lesson)
        %td= link_to 'Edit', edit_course_lesson_path(lesson.course, lesson)
        %td= link_to 'Destroy', course_lesson_path(lesson.course, lesson), method: :delete, data: { confirm: 'Are you sure?' }

