.card
  .card-header
    %h1 Editing lesson

  .card-body
    = render 'form'

  .card-footer
    = link_to 'Show lesson', course_lesson_path(@course, @lesson), class: "card-link"
    = link_to 'Back to course', course_path(@lesson.course), class: "card-link"
